# Copy to config/.env and fill in values.
# config/.env is gitignored — NEVER commit this file.

# --- Brandon Medina demo stack (typed health MCP + synthetic CSV data) ---
HEALTH_DB_PATH="~/.openclaw-health-assistant/final_project.db"
HEALTH_ACTIVE_USER_ID=PT0001
# Apple Health QR sync writes into the same file (keep in sync with HEALTH_DB_PATH):
OPENCLAW_HEALTH_DB_PATH="~/.openclaw-health-assistant/final_project.db"

# --- Remote FHIR MCP (optional; superseded by demo health MCP for class) ---
FHIR_MCP_BASE_URL=https://mcp-fhir-server.com
FHIR_MCP_API_KEY=
FHIR_PATIENT_FIRST_NAME=Ruben688
FHIR_PATIENT_LAST_NAME=Waters156
FHIR_PATIENT_DOB=1972-08-02

# --- Apple Health: QR pairing (optional) ---
APPLE_HEALTH_API_BASE_URL=
APPLE_HEALTH_DEVICE_TOKEN=

# --- Ollama / OpenClaw ---
OLLAMA_API_KEY=ollama-local
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_TOOLS_MODEL=qwen2.5:7b
