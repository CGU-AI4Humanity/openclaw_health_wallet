# Demo synthetic data (Brandon Medina Final_Project)

CSV fixtures under `csv/` seed `final_project.db` via `seed_database.py`.

```bash
./scripts/seed_demo_database.sh
```

Default output: `~/.openclaw-health-assistant/final_project.db` (`HEALTH_DB_PATH` in `config/.env`).

30 synthetic patients; default active user `PT0001` for the health MCP.
